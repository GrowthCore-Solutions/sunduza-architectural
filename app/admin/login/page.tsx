// TODO: Admin login page — email + password form using NextAuth signIn("credentials")
//       Redirects to /admin on success, shows inline error on failure
export default function AdminLoginPage() {
  return null;
}
