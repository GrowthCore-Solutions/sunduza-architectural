// TODO: Admin dashboard detail view — bookings table with status badges and inline actions
//       Status flow: new → contacted → in_review → confirmed → completed | cancelled
export default function AdminDashboardDetailPage() {
  return null;
}
